function argumentsLength(...args){
    return args.length;
}

//the (.....)in the function is called ares parameter.
//tis feature allows the function to accept an indefinite 
//number of arguments and gathers them into an array
